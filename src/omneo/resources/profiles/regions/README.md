### Get Profile Regions

```javascript
const profileId = 'profile123';

omneoClient.profiles.regions.list(profileId)
    .then((data) => {
        // handle response data
    })
    .catch((error) => {
        // handle error
    });
```

